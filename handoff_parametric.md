# handoff — parametric surface

## Engineering queue (next action first)

1. **Profile the superlinear translator/engine cost** (×4 N → ×12 wall at
   N=16384, net of the expander — `experiments/parametric/`). Declared
   families sidestep it; it stands for every non-certified event.
2. **A declared route for declarations**: the Θ(N) enumerated cell
   declarations dominate large-N wall (~4 s at N=2^20).
3. **The schedule consumes the head-folded form natively**: cache the
   block fixpoint per (shared operation, shared block) — O(log N)
   distinct computations end to end, not just O(log N) construction.

## Theory queue

1. Scalarset discipline / orbit tags (quotient reduction): deferred
   (sketch in the retired spec, git history).

## Blockers

None.
