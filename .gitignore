# Fetched dependencies (never vendored: see cmake/hsc_deps.cmake)
/deps/

# Build trees
/build/
/build-*/

# Diagnostic logs (kept in-folder, never committed)
*.log

# Paper library (pdf + txt extracts), fetched from the web, never committed
/papers/

# The CI build script's own trees (build_hsc.sh)
/usr/
/website/
/expat-*/
/expat-*.tar.gz

# test run outputs (tests/pn_samples.sh and friends)
/tests/logs/
